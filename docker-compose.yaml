version: "3.8"

services:
  docchatter:
    build: .
    container_name: docchatter
    ports:
      - "8501:8501"
    environment:
      PORT: 8501
      PYTHONPATH: /app
    restart: unless-stopped
